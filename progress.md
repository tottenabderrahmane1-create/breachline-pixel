Original prompt: I want to create An entire game it should be basically an exact replica of door kickers 2 But instead of having like real looking characters It should be like kind of like pixel art characters generate all assets all images and everything like that Generate them use AI and everything and make a full playable game It should be really detailed should have add-ons should have everything I want you to research really deeply do everything that is needed and I should have a full playable game. It should be able to run on the web as well brought in my browser as well and save data and everything

Progress:
- Created a new original browser game project at /Users/28atotten/BreachlinePixel.
- Researched high-level genre pillars: real-time with pause, path planning, mission/editor/mod support, custom content, and procedural replayability.
- Generated an AI pixel-art asset sheet and copied it into assets/ai-generated-pixel-asset-sheet.png.
- Planned original design: Breachline Pixel, a pixel-art tactical breach game with no copied Door Kickers 2 art, maps, UI, names, or assets.
- Implemented static web app shell, canvas renderer, tactical game engine, four-operator squad, path planning, pause/go flow, line-of-sight combat, doors, flash/smoke/breach/medkit tools, campaign missions, localStorage save data, add-on toggles, mission import/export, and an editor.
- Ran `node --check src/game.js`; syntax passed.
- Started local server with `npm run dev` on http://127.0.0.1:5173.
- Verified with the required web-game Playwright client: start screen renders, mission launches, path orders queue, live mode advances, doors open, combat changes state, screenshot and text state are generated, and no web-game-client error file is present.
- Verified with browser automation: favicon/console clean, add-on sample mission imports, editor export produces valid 20-row by 30-column JSON, and a tiny editor mission completes successfully with an A grade/save path.
- Upgraded gameplay rendering to use generated art directly: created assets/runtime-atlas.png from the AI sheet and wired it into units, doors, crates, windows, floors, walls, smoke, muzzle/flash effects, objectives, extraction, medkits, and UI tool icons.
- Added focus/overwatch orders, recon drones, enemy tagging, suppression, enemy roles, armor and weapon piercing, visual focus lines, drone scan rings, pressure pips, and expanded text-state reporting.
- Used the Browser plugin to reload and inspect the local app; verified the upgraded mission UI exposes Focus, Drone, and Intel status.
- Re-ran the web-game test client; state reports runtimeAtlas=true and generatedSheet=true, gameplay screenshot shows generated assets in active missions, and no errors were reported.
- Ran a targeted drone/focus Playwright scenario; confirmed focus tile, active drone, tagged hostile, and no console/page errors.
- Added closer tactical camera with transformed input mapping, fog-of-war visibility sets, hidden room interiors, hidden objectives/enemies in render_game_to_text until visible/tagged, stronger connected wall rendering, and a small camera HUD.
- Added operator under-fire reactions: incoming enemy shots set underFire/reaction state, snap focus toward the shooter, increase suppression, log contact, and can trigger a short break-to-cover step.
- Verified in the Browser plugin that the app reloads and mission UI still works after the fog/camera update.
- Re-ran required web-game client after final changes; initial Training Block state shows zoom=1.62, visibleTiles=142, hiddenActiveEnemies=3, hiddenObjectives=1, enemies/objectives hidden from text state, and no errors.
- Ran a targeted reaction scenario; Rook moved into contact, got hit, focus snapped toward the shooter, suppression rose, underFire was active, and no console/page errors occurred.
- Implemented the Breachline Pixel logo asset and full-screen loader overlay. The loader preloads the generated sheet, runtime atlas, and logo, reports progress, then fades into the app shell.
- Expanded the armory to real-name, game-balanced weapons: M4A1, HK416, MP5A5, MP7, AK-74M, Benelli M4, M110 SASS, Glock 17, and M249 SAW. Added compatibility filtering, weapon cards, stat bars, and backward-compatible old gear aliases.
- Converted operator paths into waypoint command nodes. Added queued Smoke, Flash, Breach, Drone, Focus, Wait, and Clear Room actions that execute when the operator reaches the selected waypoint and deduct inventory only at execution time.
- Added waypoint action icons on the canvas and queued action summaries to `render_game_to_text`, along with camera world coordinates, smoke/door state, logo asset readiness, hidden stunned counts, pending planned action state, and enemy shared-intel state.
- Upgraded enemy AI with shared sightings/noise memory, breach/flash/smoke investigation signals, deterministic mission RNG for combat rolls, and role-specific behavior for Sentry, Rusher, Armored, Flanker, and Suppressor enemies.
- Verified with Browser plugin reload: loading screen appears/fades, logo renders from `assets/logo.png`, and no browser console errors were reported.
- Re-ran `node --check src/game.js`; syntax passed after the full upgrade.
- Re-ran the required web-game Playwright client after the upgrade; Training Block launches with generated assets/logo loaded, close camera and fog intact, and no runner errors.
- Ran focused Playwright scenarios: queued smoke stayed queued until Rook reached waypoint then deployed; queued breach opened the target door and set shared breach noise/alert; HK416 armory selection persisted across reload, then Mako was restored to M4A1.
- Fixed Safari loading freeze at 0% by removing newer JavaScript syntax (`?.`, `??`, `||=`, and `replaceAll`) from `src/game.js` and cache-busting the module URL in `index.html`.
- Verified the Safari-compatible script with `node --check src/game.js` and the required web-game Playwright runner at `http://127.0.0.1:5173/?safari_fix=1`; Training Block loads with assets/logo ready.
- Updated fog/camera usability: previously seen tiles now stay readable instead of re-fogging, extraction gets a remembered reveal bubble, and render text reports `extractionAreaRemembered`.
- Added map navigation controls: Pan tool, drag-to-pan while Pan is active, mouse wheel zoom, WASD/arrow panning, +/- zoom, C recenter, and follow/free camera state in HUD/text.
- Adjusted waypoint clicks so the selected operator can rally to another operator's current position or to another operator's existing path node instead of only changing selection.
- Verified with `node --check src/game.js`, the required web-game runner at `http://127.0.0.1:5173/?map_fog_pan=1`, and a focused Playwright test for panning, recentering, rallying to Mako, rallying to Rook's route, extraction reveal, and no console/page errors.
- Added generated custom portraits for Rook, Mako, Lynx, and Volt, preloaded them in the game loader, and surfaced them in selected-operator, squad, and armory UI.
- Added a more professional mission architecture layer: mission rooms, tags, room labels on the canvas, mission intel strips, and two new authored operations (`Embassy Annex` and `Metro Switch`) with glass angles, courtyards/platform lanes, and named spaces.
- Added commander-grade controls: Squad Move plots all operators to a formation, Stack sends the squad to door/target stack positions, Halt All clears all paths/focus, and the canvas now draws a minimap with camera viewport, seen terrain, visible contacts, operators, and extraction.
- Verified the real-game pass with `node --check src/game.js`, the required web-game runner at `http://127.0.0.1:5173/?real_game_pass=1`, focused Playwright checks for portraits/Squad/Stack/Halt All/new missions, and Browser plugin reload/open of Embassy Annex with no console errors.
- Added fireteam selection: group chips for All/Alpha/Bravo/Leader, per-operator group toggles, V/drag box selection, Shift+number additive selection, 0 select all, group move/focus/stack behavior, Halt Group, Clear Group, multi-selected path highlighting, and selectedGroup text-state reporting.
- Rebuilt generated raids into curated blueprint presets instead of random room rectangles. Added civic office, pump house, and rowhouse blueprint variants with authored rooms, doors, windows, cover, starts, enemy anchors, objectives, extraction, and room labels.
- Added mission layout repair/audit: every base/custom/generated mission is validated before play, sealed room interiors are opened, blocked markers/doors are repaired, and `audit_mission_layouts()` reports starts, rooms, doors, and unreachable counts.
- Upgraded older campaign maps with room metadata, mission tags, and validation so Market Rescue, Server Vault, Night Depot, and Dockside Relay now behave like authored layouts instead of unlabeled map blocks.
- Verified the group/map pass with `node --check src/game.js`, the required web-game runner at `http://127.0.0.1:5173/?group_map_pass=1`, a focused Playwright audit showing all built-in missions connected with 0 unreachable targets/rooms/exits, Alpha group move/halt checks, a screenshot at `output/web-game/group-map-pass/group-map-ui.png`, and Browser plugin reload of Blueprint Raid with no console errors.
- Added generated tactical assets (`stairs`, `shield`, `route`, `command`) and integrated them into the loader, map renderer, and maneuver UI.
- Added a real tactical maneuver layer: Dynamic Entry, Slice Pie, Bounding, Smoke Cross, Stair Sweep, Rescue Escort, and Regroup Cover. Maneuvers score the current context, recommend a tactic, queue waypoint actions, plot formation movement, and monitor active tactics for contact/suppression/rescue-state adaptation.
- Added the `Clinic Tower` mission: a real-world two-level hospital rescue with ground-floor entry, protected stairwell, second-floor patient ward/ICU, roof-access objective, civilian rescue, and extraction through the ambulance bay. The map uses stair tiles and `levels: 2` metadata.
- Added maneuver/advice state to `render_game_to_text`, including recommended tactic, reason, active maneuver timers, layout levels, stair counts, scenario text, and tactical asset readiness.
- Verified the tactical maneuver pass with `node --check src/game.js`, the required web-game runner at `http://127.0.0.1:5173/?tactical_maneuver_pass=1`, a focused Playwright scenario for All selection, Dynamic Entry queued actions, Stair Sweep paths, no console errors, and Browser plugin verification that `Clinic Tower` has 2 levels, 8 rooms, 16 doors, 6 stair tiles, 0 unreachable areas, and all tactical assets loaded.
- Implemented the Polish + Realism pass: added an original full-screen home hub/start menu, generated key art and tactical UI assets, explicit loader hiding for Safari, a shared command registry, cleaner icon-led side tools, and a Q radial tactical menu with Move, Stack, Focus, Smoke, Flash, Door Breach, Window Breach, Drone, Clear, Regroup, and Cancel.
- Added architecture realism checks and reporting: `render_game_to_text` now includes screen/radial/audit/window state, `audit_mission_layouts()` reports architectural warnings, and all current built-in/custom/generated missions audit connected with 0 unreachable targets and 0 warnings.
- Added `Step Breach Window`: breached windows become passable, create breach noise, alert/stun nearby enemies, render with a custom breached-window asset, and are understood by Dynamic Entry as valid entry openings.
- Verified the polish pass with `node --check src/game.js`, a Safari syntax scan for optional chaining/nullish/logical assignment/replaceAll, Browser plugin checks at `http://127.0.0.1:5173/?ui_realism_pass=1`, targeted Playwright checks for radial focus, queued smoke execution, window breach/pathing, no console/page errors, and the required web-game runner with screenshots in `output/web-game/ui-realism-pass-final`.
- Added the game polish/sync pass: replaced the original mark with a richer generated logo, added a wide logo lockup, upgraded the home hub branding, renamed operations to stronger campaign-style names, and cache-busted the module URL for reliable browser refreshes.
- Synced the editor with the full campaign pipeline: title/brief/objective/difficulty/tags/levels/night/scenario fields, inferred room metadata, architecture validation, repair layout, load-current-operation, export/import metadata, save to custom missions, and test-launch into the actual campaign view.
- Rebuilt the default editor canvas into a ready-to-edit safehouse blueprint with rooms, doors, windows, cover, starts, contacts, objective, and extraction instead of an empty floor.
- Verified the game polish/sync pass with `node --check src/game.js`, Safari syntax scan, Browser plugin UI checks, a broad no-console-error Playwright scenario covering home/armory/radial/editor/export/save/test-launch, and the required web-game runner with screenshots in `output/web-game/game-polish-sync`.
- Game-feel pass: added screen shake on operator hits, breach (door + window), flashbang, and mission failure; added per-operator hitFlash with a red impact pulse; added a 5-line HUD kill feed; rebuilt the mission-end overlay into a framed stats panel (time, hostiles down, wounded/down operators, civilians extracted, payout).
- Logo rework: replaced `assets/logo.png` with a higher-contrast pixel shield + chevron breach beam + crosshair core + BP wordmark plate, refreshed `assets/logo-lockup.png` to match, and redrew `assets/favicon.png` with the same chevron mark.
- Mission name pass: renamed procedural blueprints to `Operation Civic Warrant <HEX>`, `Operation Pump Hall <HEX>`, `Operation Rowhouse Lock <HEX>` (4-digit hex seed), added scenario text to each, and renamed the editor default to `Operation Workbench` with a clearer "Save or Test" hint.
- Editor sync: editor `Test` now also saves the mission first, and the editor inspector lists every saved custom mission with Load / Play / × controls so the editor stays in lockstep with the campaign panel.
- Cache-busted the script URL to `?v=game-feel-sync-2`, re-ran `node --check src/game.js`, and re-ran the Safari syntax scan (`?.`, `??`, `||=`, `replaceAll`) with no matches.
- Created `ASSETS_TODO.md` to brief Codex / image-gen on the higher-fidelity raster assets that still need painted versions (atlas repaint, operator portraits, hero key art, mission banners, weapon icons, VFX spritesheets), with palette tokens, sizing, naming, and wire-up notes.
- Enemy hold-position pass: every enemy now carries a per-role `leashRange` (sentry 80px, armored 130px, suppressor 170px, flanker 320px, rusher 360px) and an explicit `guard` anchor with a guard facing. `enemyObjective()` filters all sightings / noise / regroup targets through the leash for sentries and armored, so they no longer chase across the map. The wander branch returns them to guard, and when squad alert is up they peek-rotate their facing toward the threat instead of swaying randomly.
- Enemy squads + shared intel: `assignEnemySquads()` clusters spawned enemies into squads by proximity (5 tiles, BFS). `state.enemyIntel.squadAlert[squadId]` tracks per-squad alert level (decays at 0.25/s). `recordEnemySighting()`, breach/flash/window-breach/smoke noise inside earshot, post-stun recovery, and operator kills all bump the right squad's alert. The whole squad reacts even if only one squad-mate saw or heard the tactic.
- Enemy reinforcement: when an operator drops an enemy, `state.enemyIntel.reinforce = { x, y, t: 7 }` is set. Rusher / Flanker / Suppressor roles prioritise that point in `enemyObjective`, so killing one hostile pulls in nearby movement-roles for a few seconds — solo Dynamic Entry now meets a reinforcing fireteam instead of static targets.
- Tactic-resistance pass: flash stun is role-scaled (armored 1.4s, sentry 2.1s, others 3.3s, always Math.max-ed so chains don't shorten). When stun wears off, the enemy gets a 2-second `postStunBoost` that tightens spread to 0.09 and shortens reload by 22%. `fireWeapon()` also applies squad-alert reload/spread multipliers (alert >0.6 → ×0.86 cooldown / 0.11 spread; alert >1.5 → ×0.72 cooldown / 0.09 spread), and `enemyThinkDelay()` cuts pathing latency by 20–40% under squad alert. The breach/flash/smoke noise channel earshot is 1.6× radius for squad alerts so the whole nearby fireteam wakes up the moment a tactic fires.
- Text-state reporting: `render_game_to_text` now emits `enemyIntel.reinforce`, `enemyIntel.squadAlert`, `enemyIntel.squads`, and per-enemy `squadId`, `squadAlert`, `postStunBoost`, `leashRange` so Playwright / web-game runners can assert squad behaviour.
- Cache-busted the script URL to `?v=ai-squads-3`, re-ran `node --check src/game.js`, re-ran the Safari syntax scan (`?.`, `??`, `||=`, `replaceAll`) with no matches, and verified `index.html`, `src/game.js`, and `assets/logo.png` serve 200 from the local dev server.
- Updated `ASSETS_TODO.md` with the AI / squad data shapes Codex should be aware of when generating enemy variants and squad portraits.
- Multi-floor logic pass: added `deriveFloorMap(map, starts, mission)` that flood-fills the tile grid (walls block; stair tiles are bridges) into per-tile floor numbers (1..N) plus `"stair"` transition tiles. Wired into `startMission` so `state.floors = { map, levels }` is rebuilt on every mission load. Single-level missions still flood-fill so the API is uniform.
- Per-entity floor tracking: every operator and every enemy carries a live `level` field. `updateOperator()` recomputes `op.level` each tick from `entityLevel()`; when it changes (i.e. operator crosses a stair tile), it logs `"<Name> moved to floor N."` and plays a soft transition tone. `updateEnemy()` does the same so hostile tracking on the minimap respects floors.
- Floor HUD: mission intel strip now reads `Floor X/Y` based on the selected operator; the per-operator squad rows show `Lead | L1` / `L2` badges when a mission has multiple levels.
- Minimap: tiles on the inactive floor are dimmed (55% black overlay), stair tiles are tinted cyan, operator and enemy pips on the inactive floor render at 45–50% alpha, and a `Floor X/N` badge is drawn under the minimap. The active floor is whichever floor the focused operator currently stands on.
- `render_game_to_text` now emits `missionIntel.floors = { levels, stairTiles, tilesPerFloor: { floor1, floor2, stair }, activeFloor, operatorFloors: [{ id, level }] }` so Playwright / web-game runners can assert multi-floor behaviour.
- Cache-busted the script URL to `?v=floors-4`, re-ran `node --check src/game.js`, re-ran the Safari syntax scan (`?.`, `??`, `||=`, `replaceAll`) with no matches, and verified `index.html` and `src/game.js?v=floors-4` serve 200 from the local dev server.
- Codex asset pickup pass: switched the UI loader from sheet-slice coordinates to standalone PNGs (`logo.png`, `logo-lockup.png`, `favicon.png`, `home-keyart.png`, `op-{rook,mako,lynx,volt}-portrait.png`, `stairs.png`, `shield.png`, `route.png`, `command.png`, `radial-center.png`, `window-breach.png`, `wall-{connector,single,end,straight,corner,t,cross}.png`). Added an `imageAssets` map of slug → `Image` and an `imageAssetSources` map of slug → URL. `uiSpriteStyle`, `drawSheetAsset`, `drawGeneratedAsset`, and `iconMarkup` now prefer the standalone file, falling back to the master `generated-ui-asset-sheet.png` slice only if the standalone is missing.
- Bug behind the broken portraits: the old code sliced from hard-coded x/y on a 2048×1024 sheet that didn't match Codex's actual sheet layout, so the four cropped portraits Codex emitted (which are themselves correct 96×96 cards on dark backgrounds with the right helmet accent colours — verified via pixel-bucket analysis) were never actually drawn. The new loader bypasses the sheet entirely for any slug that has a standalone PNG, so Rook/Mako/Lynx/Volt portraits now render directly from their own files.
- `preloadAssets()` now waits for every `imageAssets` entry in addition to the master sheets, the runtime atlas, and the logo. `missingTacticalAssets()` reports a slug missing only if both the standalone and the master-sheet fallback fail. `render_game_to_text` now exposes `assets.standalone` (slug → boolean loaded) so the test runner can confirm each Codex-delivered file is reachable.
- Cache-busted the script URL to `?v=standalone-pngs-5`, re-ran `node --check src/game.js`, re-ran the Safari syntax scan (no matches), and verified the local dev server returns 200 for the bumped `src/game.js`, the master sheets, and all 21 standalone assets (`logo.png`, lockup, favicon, keyart, 4 portraits, 6 tactical icons, 7 wall pieces, plus `runtime-atlas.png` and `generated-ui-asset-sheet.png`).

- Mission redesign pass: rewrote every base/authored mission with deliberate placements instead of scattered enemies. Each mission now annotates rooms by tactical purpose (entry / overwatch / objective / flanker pocket), groups cover into sandbag posts (defender desks, sandbag stacks, objective rings), seats sentries behind their cover and armored adjacent to the objective, places rusher/flanker roles where they can intercept the squad, and threads windows along the cross-room sightlines that actually matter. Enemy counts increased: Glassline 4→5, Market Lantern 5→7, Blackbox 6→7 with three rack lanes, Nightfall 7→7 (with three container rows + window overwatch), Blackglass 6→7, Metro Cutoff 7→7 with booth-window suppression, Meridian Ward 7→9 across both floors with an armored at the stairwell choke, Dockside Signal 7→7 with a customs suppressor on a windowed east lane. Every objective now sits inside a deliberate cover ring with an armored or sentry adjacent, and every mission has a written `scenario` line describing the intended assault plan.
- Tile-budget audit (script over the redesigned mission functions): all 8 base missions report 4 starts, 1 extraction, 5–9 enemies, plus civilians where appropriate; Clinic Tower keeps 6 stair tiles for the floor-divider crossing. No tile-type collisions (e.g. enemy-on-objective) remain.
- Cache-busted the script URL to `?v=missions-redesign-6`, re-ran `node --check src/game.js`, re-ran the Safari syntax scan (no matches), and confirmed the dev server returns 200 for the bumped `src/game.js`.
- Portrait recrop pass: the four `op-{rook,mako,lynx,volt}-portrait.png` files Codex had emitted were broken crops — only 35-44% opaque with very dim helmet content, which is why the Fireteam chips rendered as empty black squares. Located the real portrait cards on `generated-ui-asset-sheet.png` (top-right quadrant at `x = 1024 + i*112`, `y = 0`, `96×96`), re-cropped from the master sheet, and verified each helmet hue matches its operator (Rook=blue h≈0.58, Mako=red h≈0.02, Lynx=green h≈0.23, Volt=yellow h≈0.11). The four standalone PNGs now show clear pixel-art cards with helmet + role badge. Cache-busted to `?v=portrait-recrop-7`.
- Found a parallel issue with the tactical icon row Codex emitted as standalone PNGs (`stairs.png`, `window-breach.png`, `radial-center.png`, `wall-connector.png`). Those crops point at different sheet content than their slug names imply (e.g. `stairs.png` shows a wall fragment, `window-breach.png` shows an X-tile, `radial-center.png` shows a route-line zigzag). These mismatches need a follow-up recrop or fresh image generation aligned to the slug semantics; logged under the Codex TODO below.

- Image cache-busting: added an `ASSET_VERSION = "7"` constant and appended `?v=${ASSET_VERSION}` to every standalone image URL (canvas Image src, CSS `background-image` in `uiSpriteStyle`, the home-hub `<img>` tags for logo/lockup/keyart, and the addons asset thumbnail). The user's browser was serving the old broken portrait/icon PNGs from cache even after the disk file changed; the version query forces a refetch.
- Stair tile is now drawn programmatically every frame instead of relying on the broken `stairs.png` standalone (which Codex emitted as a wall fragment crop). Each stair tile renders as a dark-cyan base with four light-cyan step bars and a small yellow ▲ arrow in the upper-right so the player can read "go to the other floor" at a glance. When Codex recrops `stairs.png` we can switch back to `drawGeneratedAsset("stairs", …)`.
- Floor labels on the map: `drawFloorLabels()` finds each floor's bounding box from `state.floors.map` and paints a `FLOOR N · ACTIVE` banner at the top of each side. The banner of whichever floor the selected operator is on tints gold; the other goes muted gray.
- Multi-floor refresh ticked every frame: the per-entity `level` reassignment was previously gated on `state.running` (inside `updateOperator`) so it didn't tick while paused. Moved the refresh into `update()` so it runs even paused, which means the HUD `Floor X/N` badge and the active-floor banner update the instant a selected operator crosses a stair tile (and the floor-change tone + log entry now fire from one canonical place).
- Two new authored ops:
  - **Operation Steelhouse** (difficulty 3, single floor): cartel safehouse with garage entry, vault objective NE, back den + roof-garden flank. 4 starts, 7 enemies, vault armored + sentry guarding the stash, suppressor in the den.
  - **Operation Vantage Point** (difficulty 5, two floors): police-station siege built on the same divider/stair convention as Clinic Tower. 4 starts, 10 enemies, armored at the only stair crossing, civilian informant in interrogation, captain's office is the objective on floor 2, extract through the roof bay.
- `baseMissions()` now returns 10 ops (8 authored + 1 procedural + Steelhouse + Vantage Point); Dockside Signal still ships as the add-on. Tile-budget audit re-run over all 10: every mission has 4 starts, 1 extraction, the right civilian/objective count, and the new multi-floor map has 6 stair tiles.
- Cache-busted the script URL to `?v=stairs-floor-8`, re-ran `node --check src/game.js`, re-ran the Safari syntax scan (no matches), and verified the dev server returns 200 for the bumped script and for `assets/op-rook-portrait.png?v=7`.
- Portrait `<img>` swap: even with cache-busting the `.op-portrait` `<span>` chips kept rendering as empty boxes because the rule `.op-portrait { background: #0d1211; }` in `src/styles.css` paints a dark fill, and the portraits Codex cropped have ~65% transparent pixels (rounded card corners) — so the dark fill dominated the small 34×34 chip. Switched `portraitMarkup()` to emit a real `<img class="op-portrait …">` with `src="…/op-<id>-portrait.png?v=ASSET_VERSION"` so the rasterised pixels show on top of whatever background-color the parent rule sets. `image-rendering: pixelated` in the existing CSS keeps the scale crisp.
- Three new authored ops:
  - **Operation Greenroom** (difficulty 3): nightclub. Bar street-glass, dance-floor cover columns, DJ booth (objective) on a one-way mirror, VIP lounge flanker pocket, restroom corridor for a rear approach. 4 starts / 7 enemies.
  - **Operation Cargo Hold** (difficulty 4): cargo ship. Three north-south container lanes on the main deck, bridge overwatch firing through windows, engine-room suppressor on an east slit. Bridge helm is the objective. 4 starts / 8 enemies.
  - **Operation Castiron** (difficulty 5): bank vault op. Teller-line glass, vault-foyer armored, deposit-hall suppressor, strong-room armored on the bullion. Manager-office flank lets the squad peel one defender before the direct push. 4 starts / 9 enemies (hardest single-floor map).
- `baseMissions()` now returns 13 ops in difficulty-sequenced order: Glassline · Blackglass · Market Lantern · Greenroom · Blackbox · Steelhouse · Cargo Hold · Metro Cutoff · Meridian Ward · Nightfall · Castiron · Vantage Point · daily procedural. Dockside Signal still ships as the add-on. Tile audit clean across all three new maps.
- Cache-busted the script URL to `?v=img-portraits-9`, re-ran `node --check src/game.js`, re-ran the Safari syntax scan (no matches).
- Mission unlock gating: added `missionUnlockStatus(mission, profile)` and `isMissionUnlocked(mission)`. Difficulty 1-2 ops are always unlocked; difficulty 3 needs 1 prior diff-≤2 completion; difficulty 4 needs 2 prior diff-≤3 completions; difficulty 5 needs 3 prior diff-≤4 completions. Custom and procedural missions stay unlocked. `startMission()` short-circuits with a log line if a locked mission is requested directly (so the radial, the home Continue, and any external call all funnel through the same gate). Home Hub now picks the first unlocked mission for the Continue / Play tiles and shows a `N locked` chip in the topline next to credits.
- Lock visuals: locked mission cards in the campaign panel now render with a heavy treatment — a 9-degree diagonal `🔒 CLASSIFIED` stamp (red border + glow, pulsing every 2.4s), a 135-degree red hatched overlay (police-tape pattern), a `DIFF N` tag in the top-right corner, a dashed inner border, line-through on the title/copy, and a red `🔒 Complete any …` requirement line at the bottom. Hover punches up the glow and accelerates the stamp pulse to 1.2s. All authored as a `.mission-btn.locked` rule set in `src/styles.css`, no SVG file assets needed beyond the inline padlock glyph drawn from an `<svg>` in the stamp.
- Cache-busted the script URL to `?v=mission-locks-10`, re-ran `node --check src/game.js`, re-ran the Safari syntax scan (no matches), and confirmed the dev server still returns 200 for the bumped `src/game.js` and `src/styles.css`.
- `iconMarkup()` now emits a real `<img>` whenever a standalone PNG exists (radial centre, radial actions, tool buttons, maneuver panel icons) — same fix we made for portraits. The CSS `background` fill on the icon wrappers was hiding tactical-icon content with mostly-transparent crops; the `<img>` paints the pixels on top of the wrapper so even partially transparent crops are readable. The sheet-coord fallback path is still in place for any slug without a standalone file.
- Lock card animations expanded beyond the stamp pulse:
  - A red scan line sweeps the locked card every 3.6 s via `::after`.
  - The chain border via `::before` now has corner glow dots painted by 4 radial-gradients and animates its border colour 0.35 → 0.7 → 0.35 over 8 s for a "current carrying" feel.
  - The padlock SVG inside the stamp shakes when the card is hovered (`bp-lock-shake`, 0.4 s loop).
  - The `DIFF N` corner badge flickers at ~22 % duty for a faint alarm vibe (`bp-lock-flicker`, 2.8 s).
- Three more authored ops:
  - **Operation Catacomb** (difficulty 2, single floor): underground crypt with stair-down entry, north crypt + reliquary (objective + armored), pump tunnel mid corridor, ossuary suppressor on bone piles, maintenance flank. 4 starts / 6 enemies.
  - **Operation Yardline** (difficulty 3, single floor): rail yard with two train-car cover spines splitting the yard into three lanes; control tower (objective) fires south through windows; foreman-office rear flank. 4 starts / 8 enemies.
  - **Operation Penthouse** (difficulty 5, two floors): luxury condo siege. Stairwell gated by an armored, master suite holds the civilian, panic-room armored on the safe (objective), roof suppressor on east windows. 4 starts / 10 enemies / 6 stair tiles.
- `baseMissions()` now returns 16 ops in difficulty-sequenced order: Glassline · Catacomb · Blackglass · Market Lantern · Greenroom · Yardline · Blackbox · Steelhouse · Cargo Hold · Metro Cutoff · Meridian Ward · Nightfall · Castiron · Vantage Point · Penthouse · daily procedural. With the unlock gate from the previous pass, difficulty-1-2 ops are accessible from a fresh save (Glassline, Catacomb, Market Lantern); 3+ ops require completions at lower tiers.
- Cache-busted the script URL to `?v=icon-img-locks-11`, re-ran `node --check src/game.js`, re-ran the Safari syntax scan (no matches), and confirmed the dev server returns 200 for the bumped `src/game.js` and `src/styles.css`. Tile-budget audit clean on all three new maps.
- Hardline AI reachable-spawn fix (tester-reported bug: last contact never engaged, timer froze at 2:20): Hardline used to pick random `isPassable` floor tiles, which let it spawn enemies inside isolated pockets the squad's pathing never reaches (so the hostiles-cleared check could never satisfy). Added `reachableFloorTiles()` — a BFS from every operator start through passable tiles incl. doors and stairs — and restricted Hardline's candidate pool to that set. Also filter out tiles already occupied by an operator/enemy and tiles within 4 of the extraction. `addHardlineEnemies()` now logs the actual placed count.
- Stuck-contact reveal safety net: tracked `state.lastEnemyKillTime` (reset on every operator kill, init at mission start). In `evaluateMission()`, if the objective is secured + civilians are safe + at most 3 hostiles remain + 60 s have passed with no kill, the remaining contacts get `enemy.tagged = 999` (permanent "marked") and a log entry posts: "Intel marked N remaining contact(s) for the squad." Players can finish the run; this doesn't auto-complete — it just makes invisible holdouts findable. `state.stuckRevealLogged` keeps the reveal from spamming.
- Cache-busted the script URL to `?v=hardline-reach-12`, re-ran `node --check src/game.js`, re-ran the Safari syntax scan (no matches), and confirmed the dev server returns 200 for the bumped `src/game.js`.
- Viewport freeze fix (tester-reported "timer jumped from 0:11 to 1:14 during what appeared to be a 3-second freeze"): background-tab `requestAnimationFrame` throttling was letting `state.missionTime` creep forward (each rAF clamped to 0.05 s of dt) without painting, so when the tab regained focus the canvas had to catch up to a huge time delta. Three guards in `gameLoop()`: (1) bail out without updating when `document.hidden`, (2) drop any single frame where the raw dt exceeds 0.2 s (almost always a tab resume or long stall), (3) re-anchor `state.lastFrame = performance.now()` on `visibilitychange` so the first post-resume frame starts a fresh dt. Net effect: the game pauses while the tab is hidden and resumes cleanly with no time jump.
- Stuck-contact reveal made less restrictive: the previous gate required objectives secured + civilians safe, which never fired in the tester's Glassline run because the laptop wasn't grabbed yet. Now the reveal fires in two passes — the original gentler one (60 s no-kill + ≤3 holdouts + everything else done) plus a new long-stall safety net (90 s no-kill + ≤5 holdouts, no objective gate). Log line now includes the trigger reason (`objectives clear` vs `<N>s stalemate`).
- "Clear Path" → "Cancel Orders": relabelled the side-panel button and added a `title=` tooltip so the player knows it cancels the selected operator's planned route rather than issuing a move. `Halt All` got a matching tooltip ("Stops every operator and clears all planned routes.").
- Cache-busted the script URL to `?v=freeze-stuck-13`, re-ran `node --check src/game.js`, re-ran the Safari syntax scan (no matches), and confirmed the dev server returns 200 for the bumped `src/game.js`.
- Imagegen raster polish pass: generated a new premium pixel-art master sheet with the built-in image generation tool, packed it through `tools/generate_png_assets.py`, and refreshed `assets/generated-ui-asset-sheet.png` plus the standalone PNGs for logo, lockup, portraits, tactical icons, wall pieces, window breach, and stairs. `ASSET_VERSION` is now `9` and the module URL is `?v=png-asset-polish-9`.
- Replaced remaining inline/vector UI art: the locked mission stamp now uses the generated PNG shield icon instead of inline SVG, mission stairs render from `assets/stairs.png`, editor walls use oriented wall pieces, and editor starts/hostiles/civilians/objectives/extracts prefer atlas sprites before text fallback.
- Control polish: radial Move now always targets the cursor/world point where Q opened the dial, independent of the selected side tool. Move Map keeps the explicit drag/wheel/center hint and no-order pan cursor flow.

TODO:
- Codex / image-gen: continue delivering remaining `ASSETS_TODO.md` slugs (mission banners, weapon icons, VFX spritesheets, multi-floor chips, enemy-role sprites). New slugs land in `assets/` and get added to the `imageAssetSources` map in `src/game.js` (and removed from any sheet-coord fallback that lingers).
- Future expansion ideas: patrol scripting, richer mission briefings, more generated atlas variants, a campaign map, and per-mission enemy placement metadata for even more deliberate AI setups.
